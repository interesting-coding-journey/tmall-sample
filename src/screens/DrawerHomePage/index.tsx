import * as React from 'react'import {    View,    Text,    TouchableOpacity,    StyleSheet} from 'react-native';import Icon from 'react-native-vector-icons/FontAwesome'export default class DrawerHomePage extends React.Component<any, any> {    static navigationOptions = {        drawerLabel:'首页',        //图标        drawerIcon:(props: any) => {            const tintColor = props.tintColor            return (                <Icon name="trophy" color={tintColor} size={22} />            )        },    };    render() {        return(            <View style={styles.container}>                <TouchableOpacity onPress={()=>{                    //点击打开抽屉                    this.props.navigation.navigate('DrawerOpen')                }}>                    <Text>打开侧滑栏</Text>                </TouchableOpacity>            </View>        );    }}const styles = StyleSheet.create({    container: {        flex:1,        justifyContent:'center',        alignItems:'center'    }});